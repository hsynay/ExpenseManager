# Tested with Python 3.11.9
#
# Every version is pinned so that a deploy installs exactly what was tested.
# Without pins the host installs the newest release at build time, and a new
# major version can break the app without any change in our own code.
#
# To upgrade: change a version here, run the app locally, test, then deploy.
# To refresh the whole list: pip freeze > requirements.txt (and keep these
# comments).

# --- Directly used by the app ---
Flask==3.1.3
Flask-WTF==1.3.0
Flask-Limiter==4.1.1
psycopg2-binary==2.9.12
python-dotenv==1.2.2
python-dateutil==2.9.0.post0
gunicorn==26.0.0

# --- Pulled in by the packages above (pinned for reproducible builds) ---
blinker==1.9.0
click==8.4.1
colorama==0.4.6
Deprecated==1.3.1
itsdangerous==2.2.0
Jinja2==3.1.6
limits==5.8.0
MarkupSafe==3.0.3
ordered-set==4.1.0
packaging==26.2
six==1.17.0
typing_extensions==4.16.0
Werkzeug==3.1.8
wrapt==2.3.0
WTForms==3.2.2
